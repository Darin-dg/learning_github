How to use git:

in a folder to back-up: git init

to see progress: git status

*to commit:
1) stage files for comitting: 
# add one file:
    git add 'filename"
# add everything in the folder
    git add .
2) make commit: 
get commit -m 'message written explaining the commit'

to get commit history: git log

If:
"Another git process seems..."
delete index.lock